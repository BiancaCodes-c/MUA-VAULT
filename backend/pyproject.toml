[project]
name = "mua-vault-backend"
version = "1.0.0"
description = "Python backend API for MUA Vault"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.115.2",
  "uvicorn==0.30.6",
  "python-dotenv==1.0.1",
]

[tool.uvicorn]
factory = false
host = "0.0.0.0"
port = 4000
reload = true
